const { Client } = require('pg'); const client = new Client({ user: 'postgres', password: '12345678', host: 'localhost', port: 5432, database: 'DigiWallet' }); client.connect().then(() => client.query(SELECT column_name FROM information_schema.columns WHERE table_name = 'users';)).then(res => console.log(res.rows.map(r => r.column_name))).catch(e => console.log('Error:', e.message)).finally(() => client.end());
