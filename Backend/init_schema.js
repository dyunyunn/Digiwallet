const fs = require('fs'); const { Client } = require('pg'); const client = new Client({ user: 'postgres', password: '12345678', host: 'localhost', port: 5432, database: 'DigiWallet' }); client.connect().then(() => { const sql = fs.readFileSync('Backend/database/DigiWallet_PostgreSQL_Complete.sql', 'utf8'); return client.query(sql); }).then(() => console.log('Schema imported!')).catch(e => console.log('Error:', e.message)).finally(() => client.end());
